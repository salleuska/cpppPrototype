% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_discr_function.R
\name{make_offline_disc_fun}
\alias{make_offline_disc_fun}
\title{Make offline discrepancy function}
\usage{
make_offline_disc_fun(control)
}
\arguments{
\item{control}{A list containing at least two functions:
\describe{
\item{new_data_fun}{A function \verb{function(theta_row, ...)} that simulates one
replicated dataset \verb{y*} given a single parameter draw.}
\item{discrepancy}{A function \verb{function(data, theta_row, ...)} that returns
the scalar discrepancy \code{D(data, θ)} for one draw.}
}}
}
\value{
A function of the form \verb{function(MCMC_samples, new_data, ...)}
that returns a list with two numeric vectors:
\itemize{
\item \code{obs}: discrepancies for the observed (replicate) data.
\item \code{sim}: discrepancies for the replicated data.
}
}
\description{
Creates a \code{disc_fun} that computes observed and replicated discrepancies
for each posterior draw. This is the "offline" version used when the
MCMC output does not already include discrepancy columns.
}
\details{
The returned function expects to receive MCMC samples (\code{MCMC_samples})
and the corresponding dataset (\code{new_data}). It uses the model-specific
functions provided in \code{control} to simulate replicated data and compute
the discrepancy values.
}
\examples{
\dontrun{
control <- list(
  new_data_fun = function(theta) rnorm(10, theta),
  discrepancy  = function(data, theta) mean((data - theta)^2)
)
disc_fun <- make_offline_disc_fun(control)
}

}
